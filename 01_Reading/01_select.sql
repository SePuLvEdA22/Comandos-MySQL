-- Selecciona todas las columnas de la tabla 'users'.
SELECT * FROM users;

-- selecciona unicamente la columna 'name' de la tabla 'users'
SELECT name FROM users;

-- selecciona las columnas 'user_id' y 'name' de la tabla users.
SELECT user_id, name FROM users;

